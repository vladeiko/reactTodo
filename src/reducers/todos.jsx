const state = {
  todos: [],
};

const todosReducer = (state = state, action) => {
  switch (action.type) {
    case "Add": {
      // todo.id = currentId++;
      // todo.isChecked = false;
    }
  }
};
